# Open Mic RPG — Hermes project context

When working in this repo, treat this file + `TASKS.md` as the source of truth for **current ship work**.
Deep design: `DESIGN_V2.md`. Deep systems dump: `OPEN_MIC_RPG_REFERENCE.md` (may lag UI wording).
Legacy plans under `docs/legacy/` are historical only.

`SHIP_PLAN.md` is the active pre-production intake for the final gameplay/balance changes. It is planning-only until the user finishes adding requirements and explicitly starts implementation.

## Goal

Ship everything in DESIGN_V2 by **2026-07-23**. No cuts. All special endings, hidden paths, finished ending illustrations, gallery — full scope.

## Stack / layout

- Vanilla JS + HTML + CSS (no build step)
- Main logic: `script.js` (~4.5k lines)
- Content catalogs: `content/{progression,world,events,endings}.js` (register on `window.OpenMicRpgContent`)
- UI shell: `index.html`, styles: `styles.css`
- Save: `localStorage` key `openMicRPG.save.v2` (schema V3)
- Legacy archive (survives wipe): `openMicRPG.legacyArchive.v1`
- Tests: `tests/v2-mechanics.test.js` via Node test runner

```bash
# From repo root
node --test tests/v2-mechanics.test.js
# Local play
# open index.html or any static server from repo root
```

## What is already implemented (V2 core)

- Schema V3 runs, hard reset each run, option-only legacy unlocks
- Hidden deterministic class paths now surface as one Career Crossroads choice (day 32-60, 6+ performed shows) before chosen-path Event 2 + automatic class assignment
- Mentor forks: Rossini / Gabriel early specialization + late revisit
- Crowd work (pseudo-structure in show prep)
- Tone `político`, affinity matrix hooks
- Generic + pure ending **resolution** + archive finalization
- Ending copy centralized in `content/endings.js`
- 41 automated mechanics tests — currently **all passing** (last checked 2026-08-31)
- Qualified class paths recover on days 90–99 instead of silently losing to the default ending; started class paths block run endpoints until resolved.
- The ending is an embedded `#blackScreen` main-panel view, never a popup. It currently uses a temporary performance-scene placeholder.

## Ship focus (do these, not deferred systems)

Priority order for 1.0:

1. **Ending presentation** — HTML/CSS shell is embedded in the main panel. Remaining work:
   - Ending gallery in the archive and shareable export
   - Playtest the direct illustration selection and responsive presentation
   - Sequence in `DESIGN_V2.md` § Ending Screen Presentation
2. **Playtest / balance polish** for a full 100-day run (class lock, employment, elenco, days-as-resource)
3. **Deploy / packaging** for public play (GitHub Pages / `standupsim.sitedoillan.com.br` if still the target)
4. **README accuracy** — currently claims class auto-detection "pending"; that is wrong for V2 core

## Current art scope decision

- No player avatar builder, recolorable clothing, or modular face/hair/body layers in V2. Use predefined characters.
- **Show results are not layered.** 30 finished images: `assets/scenes/results/<avatarId>/<resultId>.png` (5 tiers × 6 avatars). See `assets/scenes/results/README.md`.
- Ending illustrations stay under `assets/scenes/endings/` as finished, avatar-neutral PNGs. They are not layered or composited at runtime.
- Character visuals are done: Carvalho, Gabriel Andrade, Paulo Araújo, Stevan Gaipo, Rossini Luz, Bruno Berg, Douglas Ferreira/Douglao, and Diego Ferreira (in `assets/characters/`).

## Explicitly OUT of scope (dropped, not deferred)

- Headliner solo shows, special tape recording, Made It endgame, sets/headlinerSets — DESIGN_V2 deprecated these. Gone.

## Working rules

1. Prefer content changes in `content/*.js` over hardcoding prose/thresholds in `script.js`.
2. Ending thresholds / pure rules live in progression + resolver logic; prose in `content/endings.js`.
3. Mentor-owned unlocks (`humor negro`, `storytelling`, `prop`, `oneliner`) never unlock from levels/study/legacy alone.
4. Legacy archive unlocks **options only**, never stats.
5. Do not reintroduce removed headliner endgame without explicit request.
6. After mechanics changes: run `node --test tests/v2-mechanics.test.js`.
7. Keep Portuguese player-facing copy consistent with existing tone (Brazilian comedy circuit).
8. Update `TASKS.md` when finishing or reprioritizing work — do not dump progress into memory.

## Key code anchors (script.js)

- Save keys / schema: top constants `STORAGE_KEY`, `LEGACY_STORAGE_KEY`, `SAVE_SCHEMA_VERSION`
- Ending finalize / dialog actions: `finalizeRun`, build ending message, `Nova corrida` / history handlers
- Class / path progress: career path state + hidden threshold helpers
- Scoring engine is protected/core — change carefully and with tests

## Session kickoff checklist

1. Read `TASKS.md` (live board)
2. Skim this file
3. Only open `DESIGN_V2.md` / `OPEN_MIC_RPG_REFERENCE.md` when the task needs design or formula detail
4. Run tests before claiming mechanics done
5. Mark `TASKS.md` items complete when done
